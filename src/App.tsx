import styled from "styled-components";


const AppContainer = styled.div`
  display: flex;
  flex-direction: column;
  min-height: 100vh;
  align-items: center;
`;

const App: React.FC = () => {
  return (
    <AppContainer>
      
    </AppContainer>
  );
};

export default App;